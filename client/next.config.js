/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['res.cloudinary.com', 'i.pravatar.cc', 'nextui-docs-v2.vercel.app'],
  },
  reactStrictMode: true,
};

module.exports = nextConfig;
